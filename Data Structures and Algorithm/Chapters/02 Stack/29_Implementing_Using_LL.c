/*
Notes ==> 
1. head side will be for push and pop
2. head is now-> top
3. stackEmpty() -> top == NULL
4. stack full when -> Heap memory is exhausted ===> when pointer is again back to NULL
 or Explicit put the final limits
5. You can always set a custom size
*/